(ns merkledag.vis
  "Utilities for visualizing merkledag data webs."
  (:require
    (merkledag
      [core :as merkle])))


; TODO:
; (-> (make-graph graph)
;     (add-nodes root-1)
;     (add-nodes root-2 {:color :red})
;     (update-line root-1 root-2))
